#include <stdio.h>

#include<stdio.h>
int main() {
    int x, y, z;
    x = y = z = 0;
    x = y = 2 - 3;
    ++x, x -= 2, y %= x + 2;
    y++, Z += y + 7;
    z -= y++;
    printf( / x =%5d / ;
    Iny =%3c / lnz =%d.In
    ",x,y,z);)
    return 0;
}